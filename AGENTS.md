# Agent notes

Frozen design: `docs/SRS.md`. Realization spec: `docs/SPEC.md` and GitHub issue #1.

## Agent skills

### Issue tracker

GitHub Issues via `gh` (`DSamuelHodge/eve-fleet`). See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical names: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
